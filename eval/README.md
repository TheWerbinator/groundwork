# eval

Offline evaluation harness. A golden Q&A set over the knowledge base is run through Claude,
GPT, and a local Ollama model; each answer is scored for accuracy/groundedness and measured
for latency and token cost. Output is a comparison report that makes the cost/latency/
accuracy tradeoff concrete instead of asserted.

**Built in:** Phase 10. Stub until then.
