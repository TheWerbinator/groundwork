# apps/mcp

A Model Context Protocol server that exposes the knowledge base search plus one or two
internal tools. The LangGraph agent connects as an MCP client, so tool access goes through a
real protocol boundary rather than in-process function calls.

**Built in:** Phase 6. Stub until then.
