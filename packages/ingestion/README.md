# packages/ingestion

The ingestion pipeline: read the [knowledge base](../kb) markdown, chunk it (strategies are
swappable and compared), embed the chunks, and load them into Chroma. Shared by the agent
and the eval harness.

**Built in:** Phase 1. Stub until then.
